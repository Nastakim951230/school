<Page x:Class="school.Page.Hapka"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:school.Page"
      mc:Ignorable="d" 
      d:DesignHeight="80" d:DesignWidth="800"
      Title="Hapka">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>


        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Height="70" Source="./Image/school_logo.ico" Style="{StaticResource ImageLogo}"></Image>
                <TextBlock VerticalAlignment="Center" Style="{StaticResource LogoText}">Леарн</TextBlock>
            </StackPanel>
        </Grid>
        <Grid Grid.Column="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                <Button Name="Yslygi" Click="Yslygi_Click">Услуги</Button>
                <Button Name="admin" Click="admin_Click">Админ</Button>
                <Button Name="Zap" Visibility="Collapsed" Click="Zapisi_Click">Ближайшие записи</Button>
            </StackPanel>
        </Grid>

    </Grid>
</Page>
